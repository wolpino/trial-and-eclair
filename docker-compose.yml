services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: trial_eclair
      POSTGRES_PASSWORD: trial_eclair
      POSTGRES_DB: trial_eclair
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
