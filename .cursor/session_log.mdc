---
description: Chronological agent session history (newest first). Read top entries for continuity; do not treat as auto-applied rules.
---

# Session Log

Prepended entries from Cursor agent sessions. **Newest at top.**

For agents: read the top 1–3 entries at session start. Parse `AGENT_CONTEXT` HTML comments for structured fields.

---

## 2026-06-22 · phase-ui-c2

| Field | Value |
|-------|-------|
| **Session** | `2026-06-23T04:00:00Z` |
| **Project** | trial-and-eclair |
| **Mode** | Agent |
| **Outcome** | completed |

### Intent
Implement Phase UI C2 — cork board grid (final Phase UI chunk): PinnedNote, filters, edit drawer, promote-to-lab, pin FAB.

### Summary
Rebuilt `DeveloperHomePage` with full-bleed cork texture, status/tag filters, and CSS grid of `PinnedNoteCard` components with rotation seeded from idea id. Click opens slide-over `IdeaEditDrawer` (title, notes, status, tag, image, delete). `PinIdeaFab` replaces top create panel. Promote uses C6 `promoteIdea` API and navigates to lab spread. Removed `IdeaCard`, `CreateIdeaForm`, legacy cork styles from `index.css`. Token-only CSS in `components/corkboard/corkboard.css`. `npm run build` passes on `phase-ui-c2`.

### Decisions
- **Slide-over drawer in corkboard/** — not lab `NotebookOverlay`; keeps metaphors separate
- **Rotation via id hash** — deterministic -3°…+3°, no persisted layout (freeform deferred)
- **Tag filter dropdown** — populated from distinct `category_tag` values on loaded ideas

### Changes
- `frontend/src/components/corkboard/*`, `DeveloperHomePage.tsx`
- `frontend/src/lib/ideaRotation.ts`, `api/development.ts` (`patchIdea`, `deleteIdea`)
- Deleted `IdeaCard.tsx`, `CreateIdeaForm.tsx`

### Open threads
- Merge `phase-ui-c2` → `phase-ui-metaphor` / main (includes C5/C6)
- Freeform cork canvas deferred per `corkboard_freeform_canvas.plan.md`

<!-- AGENT_CONTEXT_START -->
**Keywords:** phase-ui, c2, cork-board, PinnedNote, promote-to-lab, pin-fab
**Files:** frontend/src/components/corkboard/*, DeveloperHomePage.tsx
**Do not redo:** C3/C4/C5/C6 surfaces; freeform canvas
**Resume with:** merge Phase UI branches; optional C6 visual polish if desired
<!-- AGENT_CONTEXT_END -->

---

| Field | Value |
|-------|-------|
| **Session** | `2026-06-23T03:00:00Z` |
| **Project** | trial-and-eclair |
| **Mode** | Agent |
| **Outcome** | completed |

### Intent
Continue Phase UI plan after C6 — implement C5 cookbooks binder, reference shelf, public OG polish.

### Summary
Rebuilt cookbooks as a binder metaphor: spine rack on index, ring-bound detail view with entry cards, add/remove published recipes, publish panel. References page uses horizontal shelf spines with type filter rail and per-type styling. Public `/r/:slug` and `/c/:slug` get `useDocumentMeta` (og:title, description, image) plus layout polish in `public.css`. Token-only CSS in new `cookbooks.css`, `references.css`, `public.css`. Build and Django tests pass on `phase-ui-c5`.

### Decisions
- **Reuse lab spine pattern** — cookbooks/references share vertical-rl spine layout with wood shelf border
- **Entry cards show snapshot version** — frozen v# badge; link to public recipe when slug known
- **OG meta via client hook** — no SSR; sufficient for share previews when crawlers execute JS

### Changes
- `frontend/src/components/cookbooks/*`, `references/*`
- `frontend/src/pages/CookbooksPage.tsx`, `CookbookDetailPage.tsx`, `ReferencesPage.tsx`
- `frontend/src/pages/PublicRecipePage.tsx`, `PublicCookbookPage.tsx`
- `frontend/src/components/RecipeViewer.tsx`, `CookbookViewer.tsx`
- `frontend/src/lib/useDocumentMeta.ts`, `frontend/src/styles/{cookbooks,references,public}.css`

### Open threads
- **C2** cork board grid last (promote button uses C6 API)
- Optional visual polish pass on C6 form-heavy areas (user deferred)

<!-- AGENT_CONTEXT_START -->
**Keywords:** phase-ui, c5, cookbook-binder, reference-shelf, og-meta, public-polish
**Files:** frontend/src/components/cookbooks/*, references/*, styles/cookbooks.css, public.css
**Do not redo:** C3/C4/C6 layout and API wiring
**Resume with:** C2 cork board on phase-ui-metaphor merge branch
<!-- AGENT_CONTEXT_END -->

---

| Field | Value |
|-------|-------|
| **Session** | `2026-06-23T02:00:00Z` |
| **Project** | trial-and-eclair |
| **Mode** | Agent |
| **Outcome** | completed |

### Intent
Commit parallel C3/C4 branches and merge them into `phase-ui-metaphor`.

### Summary
Committed `phase-ui-c4-box` (recipe box in-place index cards + removed `RecipeBoxRecipePage`) and `phase-ui-c3-lab` (lab notebook spread UI). Fast-forward merged C4 into `phase-ui-metaphor`, then merged C3 with no conflicts. Combined branch builds cleanly. Later merged to `main` via PR #16 (`aff25b0`).

### Decisions
- **Separate commits per chunk** — C4 and C3 committed on their feature branches before integration merge
- **C4 before C3 merge order** — fast-forward C4 first; C3 merge had no `App.tsx` conflicts

### Changes
- `phase-ui-c4-box` → `5fe4327`, `8bff683`
- `phase-ui-c3-lab` → `82ce437`
- `phase-ui-metaphor` → `dda9d37` (merge commit)

### Open threads
- C6 API gaps (steps CRUD, hero upload, autocomplete, test sessions) — branch `phase-ui-c6-api`
- C5 cookbooks/refs polish; C2 cork board grid last

<!-- AGENT_CONTEXT_START -->
**Keywords:** phase-ui, merge, c3, c4, phase-ui-metaphor, pr-16
**Files:** .cursor/session_log.mdc
**Do not redo:** C3/C4 feature commits; merge into metaphor
**Resume with:** C6 on `phase-ui-c6-api`; push `phase-ui-metaphor` PR if not already on main
<!-- AGENT_CONTEXT_END -->

---

## 2026-06-22 · phase-ui-c6-api

| Field | Value |
|-------|-------|
| **Session** | `2026-06-23T02:30:00Z` |
| **Project** | trial-and-eclair |
| **Mode** | Agent |
| **Outcome** | completed |

### Intent
Implement Phase UI C6: wire missing API flows (steps, hero upload, autocomplete, test sessions, idea promotion) for lab + recipe box editors.

### Summary
Added developer steps CRUD on `/api/v1/versions/{id}/steps/` (current version only) and collection box steps on `/api/v1/recipe-box/{id}/steps/`. Steps copy on save-new-version. Test session + photo APIs (max 5 photos). `POST /api/v1/ideas/{id}/promote/` creates/links `DevelopmentRecipe`. Frontend: editable steps in lab spread and recipe box cards, hero image on publish overlay, ingredient search-as-you-type, bake sessions in test log alongside journal. All 54 Django tests and `npm run build` pass on branch `phase-ui-c6-api`.

### Decisions
- **Shared `RecipeStepsEditor`** — one component for lab spread + recipe box index cards
- **Test log = bake sessions + journal** — same collapsible; sessions filtered by active version
- **Profile theme sync deferred** — C1 localStorage remains; optional `color_theme`/`font_preset` skipped for scope

### Changes
- `development/views.py`, `serializers.py`, `urls.py`, `services.py` — steps, test sessions, promote
- `collection/views.py`, `serializers.py`, `urls.py` — box steps
- `development/test_*.py`, `collection/tests.py` — new coverage
- `frontend/src/api/development.ts`, `collection.ts` — client methods
- `frontend/src/components/RecipeStepsEditor.tsx`, `AddIngredientLineForm.tsx`, lab + recipe-box UI

### Open threads
- Merge `phase-ui-c6-api` → `phase-ui-metaphor` / main
- **C5** cookbooks/refs polish next; **C2** cork board last (uses promote API)

<!-- AGENT_CONTEXT_START -->
**Keywords:** phase-ui, c6, steps-crud, test-sessions, hero-upload, autocomplete, promote-idea
**Files:** development/views.py, collection/views.py, frontend/src/components/lab/*, RecipeStepsEditor.tsx, AddIngredientLineForm.tsx
**Do not redo:** C3/C4 layout metaphors; steps placeholder stub
**Resume with:** C5 cookbooks binder + reference shelf on phase-ui-metaphor
<!-- AGENT_CONTEXT_END -->

---

| Field | Value |
|-------|-------|
| **Session** | `2026-06-23T01:15:00Z` |
| **Project** | trial-and-eclair |
| **Mode** | Agent |
| **Outcome** | completed |

### Intent
Implement Phase UI C4: recipe box wooden frame, A–Z rail, in-place index-card editing, Add card without idea.

### Summary
Created branch `phase-ui-c4-box` from `phase-ui-metaphor`. Merged `RecipeBoxPage` + `RecipeBoxRecipePage` into a single surface with in-place card expansion in the A–Z grid. Built `RecipeBoxFrame`, `AlphabetRail`, `RecipeBoxCard` using C1 `IndexCard`/`IndexCardField` scaffolds. Token-only CSS in `components/recipe-box/recipe-box.css` + `styles/recipe-box.css` import. Deep-link `/recipe-box/:recipeId` focuses card in place. Removed legacy recipe-box styles from `index.css`. `npm run build` passes.

### Decisions
- **Single route component** — both `/recipe-box` and `/recipe-box/:recipeId` render `RecipeBoxPage`
- **Grid slot wrapper** — expanded card spans full grid width via `.recipe-box-card-slot--expanded`
- **Add card default title** — empty input creates `"Untitled recipe"` (API requires title)

### Changes
- `frontend/src/pages/RecipeBoxPage.tsx` — merged box + editor
- `frontend/src/components/recipe-box/` — Frame, Rail, Card, CSS
- `frontend/src/styles/recipe-box.css` — page stylesheet import
- `frontend/src/App.tsx` — removed `RecipeBoxRecipePage` route
- Deleted `frontend/src/pages/RecipeBoxRecipePage.tsx`

### Open threads
- C6 API gaps (steps, hero upload, autocomplete)
- C5 cookbooks/refs polish; C2 cork board grid last

<!-- AGENT_CONTEXT_START -->
**Keywords:** phase-ui, c4, recipe-box, IndexCard, in-place-edit, A-Z-rail
**Files:** frontend/src/pages/RecipeBoxPage.tsx, frontend/src/components/recipe-box/*, frontend/src/styles/recipe-box.css
**Do not redo:** C1 tokens/ThemeProvider; C3+C4 merge complete
**Resume with:** C6 API gaps on `phase-ui-c6-api`
<!-- AGENT_CONTEXT_END -->

---

## 2026-06-22 · phase-ui-c3-lab

| Field | Value |
|-------|-------|
| **Session** | `2026-06-23T01:15:00Z` |
| **Project** | trial-and-eclair |
| **Mode** | Agent |
| **Outcome** | completed |

### Intent
Implement Phase UI C3 — lab notebook spread on `phase-ui-c3-lab` using C1 scaffolds; shelf + in-place notebook editing with margin tools.

### Summary
Rebuilt `DeveloperLabPage` with recipe shelf spines and prominent **New recipe → Open notebook** flow that navigates to `/developer/lab/:id`. Replaced stacked `editor-form` panels in `DeveloperRecipePage` with composition-notebook layout: left margin (version flip + tools), center `NotebookSpread` (ingredients left / steps placeholder right), right margin (version notes). Compare and publish use `NotebookOverlay`; test log is collapsible below the spread. Past versions read-only; current version editable with save + save-new-version. Token-only CSS in `styles/lab.css` and `components/lab/*`. Did not touch `index.css`, tokens, ThemeProvider, or `AddIngredientLineForm`.

### Decisions
- **Steps placeholder** — backend steps CRUD deferred to C6; right column shows stub message
- **Hero upload note in publish overlay** — C6 scope
- **Draft preserved on version flip** — switching away and back to current keeps unsaved edits

### Changes
- `frontend/src/pages/DeveloperLabPage.tsx`, `DeveloperRecipePage.tsx`
- `frontend/src/components/lab/` — RecipeShelf, VersionFlip, MarginTools, SpreadContent, LabJournal, ComparePanel, PublishPanel
- `frontend/src/styles/lab.css`

### Open threads
- C6 steps CRUD, hero upload, ingredient autocomplete, test sessions UI
- C5 cookbooks/refs polish; C2 cork board grid last

<!-- AGENT_CONTEXT_START -->
**Keywords:** phase-ui, c3, lab, notebook-spread, version-flip, margin-tools
**Files:** frontend/src/pages/DeveloperLabPage.tsx, DeveloperRecipePage.tsx, frontend/src/components/lab/, frontend/src/styles/lab.css
**Do not redo:** notebook layout structure; token-only lab CSS rule
**Resume with:** C6 on `phase-ui-c6-api`
<!-- AGENT_CONTEXT_END -->

---

## 2026-06-22 · phase-ui-c1-design-system

| Field | Value |
|-------|-------|
| **Session** | `2026-06-23T00:30:00Z` |
| **Project** | trial-and-eclair |
| **Mode** | Agent |
| **Outcome** | completed |

### Intent
Implement Phase UI chunk C1: semantic design tokens, theme/font presets, ThemeProvider, settings UI in AppLayout, and metaphor component scaffolds.

### Summary
Created `phase-ui-metaphor` branch and shipped C1 design system. Migrated all `index.css` hex values to semantic CSS custom properties; palette lives in `styles/themes/default.css`, fonts in `styles/fonts/default.css`. Added `ThemeProvider` + `useTheme()` with `localStorage` persistence and `data-theme`/`data-font` on `<html>`. AppLayout now has theme swatch + font dropdown (one preset each, extensible). Scaffolded `IndexCard`, `NotebookSpread`, and `PinnedNote` components with token-only CSS. `npm run build` passes; no hex in changed frontend files outside theme/font CSS.

### Decisions
- **Skipped Framer Motion** — optional C1 item; avoided new dependency for route transitions
- **Theme before Auth in provider tree** — appearance applies even on login/register pages
- **Inline flash-prevention script in index.html** — reads localStorage before React mount

### Changes
- `frontend/src/styles/` — `tokens.css`, `themes/default.css`, `fonts/default.css`
- `frontend/src/theme/ThemeProvider.tsx` — theme context + presets
- `frontend/src/index.css` — token imports, all colors via `var(--*)`
- `frontend/src/components/AppLayout.tsx` — appearance settings UI
- `frontend/src/components/{recipe-box,lab,corkboard}/` — scaffolds + barrel exports
- `frontend/src/main.tsx`, `frontend/index.html` — provider wiring + early theme apply

### Open threads
- C3 lab notebook spread (uses `components/lab/*`)
- C4 recipe box in-place index cards (uses `components/recipe-box/*`)
- C6 API gaps, C5 cookbooks/refs, C2 cork board grid last

<!-- AGENT_CONTEXT_START -->
**Keywords:** phase-ui, c1, design-system, theming, tokens, ThemeProvider, IndexCard, NotebookSpread, PinnedNote
**Files:** frontend/src/styles/, frontend/src/theme/ThemeProvider.tsx, frontend/src/components/{AppLayout,recipe-box,lab,corkboard}/
**Do not redo:** token migration in index.css; ThemeProvider localStorage keys (`te-theme`, `te-font`); auth shell pattern
**Resume with:** C3 lab notebook on `phase-ui-metaphor` (parallel with C4 after C1 merge)
<!-- AGENT_CONTEXT_END -->

---

## 2026-06-22 · phase-ui-auth-shell

| Field | Value |
|-------|-------|
| **Session** | `2026-06-22T23:58:00Z` |
| **Project** | trial-and-eclair |
| **Mode** | Agent |
| **Outcome** | partial |

### Intent
Start Phase UI on branch `phase-ui` from merged `phase-3-core`: authenticated PWA shell before developer lab and home-cook screens.

### Summary
Added frontend auth shell after Phase 3 backend merged (PR #13). `AuthContext` loads `/api/v1/auth/me/` on mount; login/register pages post with credentials; `ProtectedRoute` and `GuestRoute` gate routes; `AppLayout` shows role-aware nav (developer vs home cook). Stub pages for `/developer` and `/recipe-box`; public `/r/:slug` and `/c/:slug` unchanged. Extended `api/client.ts` with auth helpers.

### Decisions
- **Session cookies via `credentials: "include"`** — matches backend session auth from Phase 1
- **`requireDeveloper` on protected routes** — developer lab separate from home-cook recipe box
- **Placeholder pages for lab and recipe box** — auth shell first, feature screens next

### Changes
- `frontend/src/auth/` — `AuthContext.tsx`, `access.ts`
- `frontend/src/components/` — `AppLayout`, `ProtectedRoute`, `GuestRoute`
- `frontend/src/pages/` — `LoginPage`, `RegisterPage`, updated `HomePage`; stubs for developer/recipe box
- `frontend/src/api/client.ts` — auth API helpers
- `frontend/src/App.tsx` — routed layout with protected/guest routes

### Open threads
- Developer cork board + lab (ideas, recipes, versions, publish)
- Recipe box CRUD UI wired to `/api/v1/recipe-box/`
- Cookbooks manager, version diff UI (optional)
- Phase 4 backend still paused (import, fork, Stripe, R2)

<!-- AGENT_CONTEXT_START -->
**Keywords:** phase-ui, auth-shell, session-context, protected-routes, react-pwa
**Files:** frontend/src/auth/, frontend/src/components/AppLayout.tsx, frontend/src/api/client.ts, frontend/src/App.tsx
**Do not redo:** session auth pattern; public routes outside AppLayout; stub pages until feature work
**Resume with:** developer cork board + lab screens on `phase-ui`
<!-- AGENT_CONTEXT_END -->

---

## 2026-06-22 · phase-3-backend-complete

| Field | Value |
|-------|-------|
| **Session** | `2026-06-22T22:30:00Z` |
| **Project** | trial-and-eclair |
| **Mode** | Agent |
| **Outcome** | completed |

### Intent
Ship Phase 3 backend on `phase-3-core` in reviewable chunks: version diff, journal, cookbooks, home cook recipe box, reference library, trial/subscription gates; update docs; pause Phase 4 and hand off to Phase UI.

### Summary
Delivered six commits on `phase-3-core`: compare-versions endpoint, developer journal CRUD, cookbooks API plus public cookbook viewer, home cook recipe box (`collection/`), reference library with links (`library/`), and trial/subscription enforcement via `User.has_developer_access()` and `IsDeveloper`. Merged via PRs #12 and #13. Updated `docs/PRD.md` (v1.2) and README — Phases 0–3 marked complete. All 66 tests pass (`accounts`, `development`, `collection`, `library`). User chose to skip Phase 4 backend for now and start Phase UI in a new chat.

### Decisions
- **Subscription rules** — `active`, valid `trial`, and admin `none` allowed; expired trial date, `expired`, `cancelled` → 403
- **Phase 4 paused** — URL/scan import, fork buttons, Stripe, R2 deferred
- **Small commits per chunk** — same review workflow as Phases 1–2

### Changes
- `development/` — journal, cookbooks, compare-versions, public cookbook views
- `collection/` — recipe box API
- `library/` — references + links API
- `accounts/models.py`, `accounts/permissions.py` — developer access gates
- `docs/PRD.md`, `README.md` — Phase 3 completion docs
- `frontend/` — public cookbook route `/c/:slug` (Phase 2 scaffold extended)

### Open threads
- Phase UI: auth shell then authenticated screens (started on `phase-ui`)
- Phase 4 backend when resumed: import, fork, Stripe, R2

<!-- AGENT_CONTEXT_START -->
**Keywords:** phase-3, compare-versions, journal, cookbooks, recipe-box, references, trial-gates
**Files:** development/, collection/, library/, accounts/models.py, docs/PRD.md, README.md
**Do not redo:** Phase 3 API shapes; six-chunk commit split; subscription gate logic
**Resume with:** Phase UI on `phase-ui` — auth shell then developer lab (Phase 4 backend paused)
<!-- AGENT_CONTEXT_END -->

---

## 2026-06-22 · phase-1-developer-api-wrap-up

| Field | Value |
|-------|-------|
| **Session** | `2026-06-22T12:00:00Z` |
| **Project** | trial-and-eclair |
| **Mode** | Agent |
| **Outcome** | completed |

### Intent
Complete Phase 1 developer core backend on branch `phase-1-developer-core`: auth, ideas CRUD, recipes/versions/ingredient lines, then wrap up with docs and merge checklist.

### Summary
Shipped three reviewable commits on `phase-1-developer-core`: session auth API (`/api/v1/auth/`), developer-only ideas cork board, and development recipes with explicit `save-new-version` plus ingredient lines. Business logic for recipe creation and versioning lives in `development/services.py`. Updated README with Phase 1 API reference, test commands, and pre-merge checklist; added `media/` to `.gitignore`. All 22 tests pass.

### Decisions
- **Session auth over tokens for Phase 1** — matches DRF settings and future PWA with credentials
- **Current version only for writes** — PATCH version and ingredient-line CRUD locked to `current_version`
- **Ingredient lines at `/versions/{id}/ingredient-lines/`** — avoids 3-level nesting under recipes

### Changes
- `accounts/` — auth serializers, views, urls, permissions, tests
- `development/` — ideas + recipes API, services, serializers, views, urls, tests
- `config/urls.py` — `/api/v1/` routing
- `README.md` — Phase 1 API docs and merge checklist
- `.gitignore` — ignore `media/`

### Open threads
- Merge PR `phase-1-developer-core` → `main`
- Phase 2: publish flow, public viewer, R2 media, React PWA

<!-- AGENT_CONTEXT_START -->
**Keywords:** phase-1, auth-api, ideas, recipes, save-new-version, developer-core
**Files:** accounts/, development/services.py, development/views.py, README.md, config/urls.py
**Do not redo:** three-commit split (auth, ideas, recipes); session auth; current-version write lock
**Resume with:** merge Phase 1 PR; start Phase 2 publish + public viewer
<!-- AGENT_CONTEXT_END -->

---

| Field | Value |
|-------|-------|
| **Session** | `2026-06-20T01:07:36Z` |
| **Project** | trial-and-eclair |
| **Mode** | Agent |
| **Outcome** | completed |

### Intent
Implement session logging infrastructure: skill, dual log files, hooks, rule cleanup, and alwaysApply adjustments discussed in prior Ask-mode planning.

### Summary
Created the `session-log` skill with dual-file logging (`session_log.mdc` for substantive sessions, `qa_log.mdc` for trivial Q&A). Added `session-context.mdc` rule and `hooks.json` with `sessionStart` (read top 3 entries) and `stop` (write log) prompt hooks. Deleted duplicate `database-migrations.mdc`, kept `migration-safety.mdc` with `alwaysApply: false`. Flipped `anti-overeng.mdc` to always-on; scoped `django.mdc` and `api-design.mdc` to globs-only. Clarified Django data-migration guidance to require separate files from DDL.

### Decisions
- **Two committed log files** — `session_log.mdc` for agent continuity, `qa_log.mdc` for trivial Q&A archive
- **UTC timestamps** — `YYYY-MM-DDTHH:MM:SSZ` throughout
- **Prompt hooks on sessionStart + stop** — reliable logging without custom scripts
- **Keep migration-safety.mdc, delete database-migrations.mdc** — exact duplicates
- **anti-overeng always on; migration/api/django glob-scoped** — reduce standing context cost

### Changes
- `.cursor/skills/session-log/SKILL.md` — session-log skill
- `.cursor/session_log.mdc` — session log (with this entry)
- `.cursor/qa_log.mdc` — Q&A log header
- `.cursor/rules/session-context.mdc` — continuity rule (alwaysApply)
- `.cursor/hooks.json` — sessionStart + stop hooks
- `.cursor/rules/anti-overeng.mdc` — alwaysApply: true
- `.cursor/rules/migration-safety.mdc` — alwaysApply: false
- `.cursor/rules/api-design.mdc` — narrower globs, alwaysApply: false
- `.cursor/rules/django.mdc` — alwaysApply: false, RunPython clarification
- `.cursor/rules/database-migrations.mdc` — deleted (duplicate)

### Open threads
- Verify hooks fire in Cursor Hooks tab after reload/restart
- First trivial Q&A session will populate `qa_log.mdc`

<!-- AGENT_CONTEXT_START -->
**Keywords:** session-log, hooks, qa_log, cursor-rules, migration-dedup, alwaysApply
**Files:** .cursor/skills/session-log/SKILL.md, .cursor/session_log.mdc, .cursor/qa_log.mdc, .cursor/hooks.json, .cursor/rules/session-context.mdc
**Do not redo:** two-file split; UTC timestamps; commit both logs; migration-safety not database-migrations; anti-overeng always on
**Resume with:** normal development — hooks should auto-log future sessions
<!-- AGENT_CONTEXT_END -->

---
